/*
 * PremUI Component Template
 * --------------------------
 * Copy this folder when creating a new component.
 *
 * Structure:
 *   src/components/[name]/
 *   ├── [name].tsx           ← React component
 *   ├── [name].css           ← Styles (semantic tokens only)
 *   ├── [name].stories.tsx   ← Storybook stories
 *   ├── [name].meta.json     ← AI registry metadata
 *   └── index.ts             ← Barrel export
 *
 * Rules:
 *   1. Use semantic CSS variables — never raw hex/px values
 *   2. All interactive elements must be keyboard accessible
 *   3. Include ARIA attributes where needed
 *   4. Export from src/index.ts after completion
 *   5. Write meta.json for AI agent consumption
 */
